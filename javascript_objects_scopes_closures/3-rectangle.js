#!/usr/bin/node

module.exports = class Rectangle {
  constructor (w, h) {
    if (w > 0 && h > 0) {
      this.width = w;
      this.height = h;
    }
  }
	  const Rectangle = new print();
	const equis = 'X';
	console.log(equis);
}
